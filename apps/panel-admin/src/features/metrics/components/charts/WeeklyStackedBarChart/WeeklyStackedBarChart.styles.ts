export const WEEKLY_CHART_STYLES = {
  wrapper:      "w-full relative",
  scroll:       "w-full overflow-x-auto",
  svgContainer: "w-full",
  empty:        "flex h-40 items-center justify-center text-sm text-(--muted)",
} as const;
