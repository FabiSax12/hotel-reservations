export type SidebarTexts = {
  WRAPPER: {
    ARIA_LABEL: string;
  };
  HEADER: {
    LOGO: string;
    EXPAND: string;
    COLLAPSE: string;
  };
  ROUTES_SECTIONS: {
    SECTIONS: {
      ADMINISTRATION: string;
      RESERVATIONS: string;
      INVENTORY: string;
      FINANCE: string;
      CMS: string;
    };
    ITEMS: {
      DASHBOARD: string;
      ADMINS: string;
      RESERVATIONS: string;
      CLIENTS: string;
      ROOMS: string;
      INVOICES: string;
      CMS_LANDING: string;
    };
  };
};
