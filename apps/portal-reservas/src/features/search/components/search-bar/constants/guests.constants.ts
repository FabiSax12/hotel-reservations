/**
 * @file guests.constants.ts — Initial guest selection defaults.
 */

export const INITIAL_GUEST_SELECTIONS: { ADULTS: number; CHILDREN: number; PETS: number } = {
  ADULTS: 2,
  CHILDREN: 0,
  PETS: 0,
};
